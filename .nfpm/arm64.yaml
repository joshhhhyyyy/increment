name: "Increment"
arch: "arm64"
platform: "linux"
version: ${VERSION}
section: "utils"
maintainer: "joshy <joshua@joseos.com>"
description: "A tool to increment the latest git tag sermver by 1"
homepage: "https://github.com/joshhhhyyyy/increment"
depends:
- git
contents:
- src: "binaries/increment_arm64"
  dst: /usr/bin/increment
